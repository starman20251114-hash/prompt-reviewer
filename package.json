{
  "name": "prompt-reviewer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n core,server,ui -c yellow,blue,green \"pnpm --filter @prompt-reviewer/core build:watch\" \"pnpm --filter @prompt-reviewer/server dev\" \"pnpm --filter @prompt-reviewer/ui dev\"",
    "setup:local": "node scripts/local.mjs setup",
    "build:local": "node scripts/local.mjs build",
    "migrate:local": "node scripts/local.mjs migrate",
    "migrate:dev": "pwsh -Command \"$env:DB_PATH='dev.db'; pnpm --filter @prompt-reviewer/core migrate\"",
    "check:db:dev": "pnpm --filter @prompt-reviewer/core exec node ./scripts/check-db-schema.mjs ../../dev.db",
    "check:db:local": "pnpm --filter @prompt-reviewer/core exec node ./scripts/check-db-schema.mjs ../../data/prompt-reviewer.sqlite",
    "seed:local": "node scripts/local.mjs seed",
    "start:local": "node scripts/local.mjs start",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "ci": "pnpm run check && pnpm run typecheck && pnpm run test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^9.2.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
